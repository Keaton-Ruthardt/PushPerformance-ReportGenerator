{
  "name": "push-performance-app",
  "version": "1.0.0",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "server": "node install-chrome.js && node server/index.js",
    "client": "cd client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install --production=false && npm run build",
    "start": "node install-chrome.js && node server/index.js",
    "sync-percentiles": "node server/scripts/syncPercentiles.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "Push Performance Assessment Report System",
  "dependencies": {
    "@google-cloud/bigquery": "^8.1.1",
    "axios": "^1.12.2",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^4.2.1",
    "pdfkit": "^0.17.2",
    "pg": "^8.16.3",
    "puppeteer": "^24.33.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
